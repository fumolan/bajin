# bajin 追平 ZCode —— 进度账本（GAP TRACKER)

> 本文件是 nightly 自动化运行的唯一进度账本。每次运行：先读本文件 → 校验基线全绿 → 从 backlog 顶部取 1 个条目实现 → 测试 → 在「运行日志」追加记录。
> 人工查看也以本文件为准。

## 使命

让 bajin（本仓库，净室复刻的编码代理）在功能、体验、稳定性上追平并超越 ZCode v3.7.7。

## 完成标准（全部满足即达标，停止开发）

1. 功能面：下方 backlog 全部条目完成或明确标记「不做+理由」
2. 测试：`pnpm -r test` ≥ 150 项且全绿
3. 内核：单文件 bundle（dist/bundle/bajin.cjs）< 1MB，CLI 冒烟通过
4. 桌面：AppImage 打包通过，打包态 app-server 协议冒烟通过
5. 文档：README 与本账本与实际功能一致

## 硬性约束（每次运行必须遵守）

- **净室原则**：只参考 ZCode 的行为与接口（设计文档 + 参考/ 源码快照），绝不复制其代码文本
- **git 策略**（2026-08-16 用户更新）：每轮全绿后做一次**本地** commit（防回滚/灾难性更新，仓库在 bajin/ 自有 .git，父级仓库勿动）；**禁止 push**、禁止代替用户改历史
- 不修改 `参考/`、`~/.zcode/`（只读）；不引入未声明的全局工具
- **全绿底线**：任何时刻 `pnpm -r build && pnpm -r test` 必须通过；一次运行只做 1-2 个可验证增量，宁小勿破；大特性切最小可用切片
- **执行时间戳（2026-08-17 用户新增）**：每轮最终报告的**第一行**必须输出执行时间——运行 `date '+%Y-%m-%d %H:%M:%S %A'` 并原样粘贴（不让模型自己推算时间）；运行日志表的时间列同样用该命令取值；本地 git commit 的时间戳作为第三方核验来源（`git log --format='%h %ci %s'`）
- **用户可检查性（2026-08-16 用户批评后新增）**：每轮产出必须可被用户在启动 AppImage 后验证——凡涉及 core/cli 且面向用户的变更，当轮必须重打 AppImage（scripts/package.sh，不能只 bundle CLI）；连续两轮无 UI 可见产出时，下一轮强制选 UI 可感知条目（backlog 里 P1 体验项优先）；纯内核轮需在日志注明「本轮无 UI 变化」并给出用户验证方式（如 CLI 命令）
- 开工先修坏：若基线校验失败，先修复，本轮不做新功能

## 已完成（截至 2026-08-14）

- Agent 内核：流式循环、8 内置工具、四档权限、并行工具分组（concurrentSafe）、Edit/Write 返回 unified diff、Plan 模式（Enter/ExitPlanMode+审批）、子代理（Explore/general-purpose）、动态 system prompt（todo 回注/AGENTS.md/skills 注入/工具教练）、abort 中断、自动/手动 compact、rollout 模型 IO 日志、会话持久化（JSONL）+ --continue/--resume、skills 发现与 Skill 工具
- Provider：GLM（open.bigmodel.cn，openai 兼容，fetch+SSE，重试），默认 glm-5.3；mock/echo 测试 provider
- CLI：REPL（/model /mode /compact /sessions /status /clear）、headless -p、app-server --stdio（多会话 JSON-RPC、审批/ask-user 往返、set-allowed-tools 原地生效、status、interrupt、session/new/open/close/fork）
- 桌面：Electron 壳 + agent 子进程（ELECTRON_RUN_AS_NODE 拉起单文件 bundle）、多标签、侧边栏历史、markdown 渲染+代码复制、diff 着色、todo 实时面板、计划审批卡、审批三选（始终允许）、AskUserQuestion 原生卡、斜杠补全、token 指示、停止、分叉
- 打包：esbuild 单文件 bundle（~605KB）、electron-builder（Linux AppImage/deb、Win nsis/portable、mac dmg/zip）、GH Actions 三平台流水线
- 测试：60 项（core 52 + app-server e2e 8）全绿

## 差距 Backlog（按优先级，从顶部取）

### P0 —— 内核功能还债（ZCode 有而 bajin 缺的核心）

- [x] **自定义 slash commands**（2026-08-15 夜间运行完成）：`packages/core/src/commands.ts` 发现+展开；发现顺序对标 ZCode：用户 `~/.bajin/commands` > 工作区 `.bajin/commands`（cwd 向上到 .git 根每级扫描，靠近 cwd 优先），同名先到先得；文件名即命令名（`^[a-z0-9][a-z0-9_:-]{0,63}$` 按原始大小写校验），嵌套目录冒号命名（`review/code.md`→`/review:code`）；flat frontmatter（顶层单行，缩进/未知 key 忽略）：description/argument-hint/allowed-tools/model/skills；description 缺省取正文首个非空行，全空丢弃；展开 `$ARGUMENTS`/`$1..$9`（越界空串），有参数无占位符追加「User arguments:」，`` !` `` 动态 shell 拒绝；CLI REPL 内置命令未命中即查自定义命令执行；app-server `commands/list` RPC（补全字段）+ `send` 内自动展开（桌面端未知 `/xxx` 透传服务器）；桌面斜杠补全合并自定义命令。剩余小项：frontmatter 的 allowed-tools/model/skills 已解析未生效（挂回 P1）
- [x] **Hooks 系统**（2026-08-15 夜间运行完成，跨两次运行）：`packages/core/src/hooks.ts` HookRunner + 配置发现合并；恰好 7 事件（SessionStart/UserPromptSubmit/PreToolUse/PermissionRequest/PostToolUse/PostToolUseFailure/Stop），SessionStart 四来源 startup/resume/clear/compact；matcher 大小写敏感正则（省略全匹配、非法永不匹配），工具别名 Task↔Agent、ApplyPatch→Write/Edit 双向候选；钩子 command 型（shell，timeout 单位秒）+ process 型（argv 免 shell，timeoutMs 毫秒），超时解析链 hook.timeoutMs→hook.timeout×1000→配置 timeoutMs→60s；stdin 收事件载荷 JSON，模板与环境变量注入 BAJIN_/ZCODE_/CLAUDE_ 三前缀 PROJECT_DIR/SESSION_ID；stdout 严格 JSON（只认 decision/reason/additionalContext/continue/stopReason，多余键校验失败），退出码 0 通过/2 阻止/其他记错误不中断；Stop continue 续跑≤3（注入「继续任务」消息回环）；默认关，hooks.enabled:true 才启用；配置 = 用户 `~/.bajin/config.json` + 工作区 `.bajin/config.json`（cwd 向上到 .git 根，远→近追加，用户级在前），enabled 任一为 true 即启用；接入 Agent 全部事件点、REPL/headless/app-server 三入口；EPIPE 吞掉防偶发未处理错误
- [x] **WebSearch/WebFetch 工具**（2026-08-16 夜间运行完成）：`packages/core/src/tools/web.ts`；WebFetch（http(s) 校验、AbortController 超时 30s、512KB 上限、HTML→纯文本「源码空白压平+换行仅由块级标签产生」确定性算法、raw/text 双模式、20k 字符截断）+ WebSearch（免 key DuckDuckGo html 端点，uddg 跳转链接还原、`web.provider: duckduckgo|off` 可关）；UA `bajin-WebFetch/0.1`；注册 builtinTools（readOnly/low 风险/可并发）；fetchImpl/timeoutMs 全量注入测试 7 项不触网
- [x] **MCP client**（2026-08-15 人工批次完成，build 23/26）：stdio + sse 双传输运行时（`core/mcp.ts`，newline JSON-RPC / HTTP+SSE endpoint 协商），工具命名 `mcp__<server>__<tool>`，多 server 并联单点失败隔离；配置在 `~/.bajin/config.json mcpServers`（桌面端 Agent 设置有 CRUD UI）；假 stdio server + 本地 SSE server e2e 共 5 项测试
- [ ] **SQLite 会话库**（2026-08-16 夜间两切片完成）：①`--rewind <n>` 已完成——rewindTranscript + CLI 接线（rewind.test.ts 4 项）②**SessionStore 模块已完成**——`core/session-store.ts`：node:sqlite 惰性加载（createRequire 运行时解析：兼容 vitest / CJS bundle / Electron 缺模块不炸 core）、五表 schema（session/message/part/todo/tool_usage，WAL+索引+外键级联）、storeAppendMessage（事务内拆 part=assistant.toolCalls 与 tool 结果、tool_usage 统计行）、storeLoadTranscript（与 loadTranscript 同压缩回放语义）、storeListSessions、migrateJsonlToStore（meta 兜底/损坏行跳过/幂等跳过已入库），session-store.test.ts 4 项（迁移后与 JSONL 回放逐条一致、幂等重迁移、写入拆表、空目录安全）；**写路径双写已完成**（2026-08-16 夜间切片4：AgentOptions.storeSink 双写钩子——persist() 与 compacted 标记并行入 store、异常只吞不阻断；app-server 进程级惰性开库 <BAJIN_HOME>/sessions.db（失败降级纯 JSONL 告警一次）、createSession upsert session 行（含 title/cwd/model/createdAt）+ storeSink 更新 modified_at；session 表补 group/pinned/modified_at 列——旧库自动 ALTER 补列、upsert 未显式提供不覆盖旧值（pinned 可空=NULL 表未提供）、迁移器带 group/pinned 且 modifiedAt 取最后消息 ts；session-dualwrite.test.ts 3 项：跑一轮 mock 对话后 store 回放与 JSONL 逐条一致+modified_at 非空、新列 upsert 语义、旧五列库自动补列）；**元数据/清理闭环已完成**（2026-08-16 夜间切片5：storeUpdateSessionMeta（只动提供的键、group null=清除）/storeDeleteSession（级联清消息 part todo usage）/storeReplaceTodos+storeLoadTodos（todo 快照整表替换）；app-server 四处接线——session/set-group 与 rename/pin 的 sessionMutateMeta 写 meta.json 后同步 store、sessionDelete 双清、bindCallbacks 的 todo-updated 事件处 storeReplaceTodos live 写入、listSessionsEnriched 加 store 覆盖层（title/group/pinned 以 store 行为优先、store 不可用回退纯 JSONL 行为不变）；session-meta.test.ts 3 项）；**读路径收官已完成**（2026-08-17 夜间两轮，中断轮已补报）：session/open 历史读优先 store（agent.resumeFromMessages，有消息走 store 否则回退 JSONL）——JSONL 损坏可容灾恢复；根因修复 requireInit 以 sessions.size 判定导致「关闭全部会话后 open 被误拒」→ initializedOnce/requireBoot；e2e 容灾用例透出 open error；**剩余（可选）**：JSONL 停写开关；`bajin migrate` 子命令已完成（2026-08-16 夜间：`bajin migrate [--db <file>]` 默认 `<BAJIN_HOME>/sessions.db`，幂等并报告计数；修 session-store 在 CJS bundle 下 createRequire(import.meta.url) 失效→execPath 回退；migrate.e2e.test.ts 2 项）
- [x] **settings 作用域链**（2026-08-16 完成）：core/settings.ts——mergeSettingsLayers（深合并、数组整体覆盖、undefined 跳过）/discoverProjectConfigFiles（自 cwd 逐级向上到含 .git 的目录为止收集 bajin.json 与 .bajin/config.json，远→近排序，无 .git 只取本级）/loadSettingsChain（System<User<Project 组装，损坏与非对象文件在读取层跳过）/envSettingsOverlay（BAJIN_MODEL/MODE/BASE_URL/ALLOWED_TOOLS/DISALLOWED_TOOLS）；cli/config.ts loadConfig 切换到该链（DEFAULT_CONFIG=System 层 + Env 层叠加，删除本地 mergeDeep；Session/Cli 层由调用方 overlay——main.ts 旗标已是事实 Cli 层）；settings.test.ts 3 项（.git 根双文件+中间级+cwd 全矩阵与 .git 外不收集、无 .git 本级-only+env 空值忽略、merge 语义）
- [x] **后台任务**（2026-08-16 完成，P0 清零）：core/background.ts BackgroundTaskManager——进程内任务表（32 上限、挤最旧已结束）、环形缓冲保尾部 64KB、退出码/结束时间、stop=SIGTERM+2s SIGKILL 兜底；Bash 加 run_in_background（spawn 即返 taskId+前 5 行）；TaskOutput（block 默认真 30s 上限轮询收尾、false 立即快照）、TaskStop（已结束/未知 id 明确提示）；background.test.ts 4 项（启动即返+阻塞等退出码、非阻塞 peek+终止长任务、错误提示、缓冲 cap 保尾部）；app-server 事件转发（task-notification 推送）留 P1

### P1 —— 体验追平（桌面端 ZCode 有的面板/能力）

- [ ] 桌面端 **终端面板**（node-pty + xterm.js，底部抽屉）
- [ ] 桌面端 **文件树**（浏览 cwd、点击让 agent Read、脏标记）
- [ ] 桌面端 **系统通知**（任务完成/被拒时 Notification API）
- [x] **图片 Read**（2026-08-17 夜间完成）：tools/image.ts parseImageSize——PNG(IHDR BE32)/GIF(LE16)/JPEG(扫 SOF0-15 段，正确按 len 跳段)/WEBP(VP8X 24bit-1 精确 + VP8/VP8L 14bit 尽力)，各格式自带最小长度守卫（GIF 头仅 10B）；readTool 图片扩展名分支：读头 64KB 解析，返回「[图片] 路径 / 格式·宽×高·大小 / 多模态预留说明」，替代原二进制报错；formatImageDescription 两态（解析失败仍给大小）；导出 API 供将来多模态接线。image-read.test.ts 3 项（四格式手工头解析/非图片与截断 null/Read 分支与 txt 对照——顺带修测试构造器的 JPEG 段布局错误）
- [x] **AskUserQuestion multiSelect 全链路**（2026-08-17 夜间完成）：shared/core/app-server 三层本就透传 multiSelect，断点在渲染层——ask-card 加 multiSelect 分支 AskMultiCard（选项复选 ☑/☐+选中态样式+「确认（已选 N 项）」按钮，答案按选项序「、」拼接，0 项禁用；自由输入框仅单选保留）；Tab.ask 类型与事件 cast 补 multiSelect；e2e 新增 1 项（事件带 multiSelect→多选「lint、test」回传→tool-result 注入拼接值）
- [x] **EnterWorktree/ExitWorktree**（2026-08-17 夜间完成）：tools/worktree.ts——Enter 建 .bajin-worktrees/<name>+分支 bajin/<name>（基线可指定，默认 HEAD）并 setCwd 切会话目录；Exit 切回 initialCwd（remove=true 顺带 worktree remove+branch -D，未合并改动被 git 拒绝时如实报告）；主仓库根定位用 --git-common-dir（worktree 内 --show-toplevel 返回自身）；Agent 新增 setCwd（更新 opts.cwd+refreshSystem）与 initialCwd；注册动态工具集。worktree.test.ts 2 项（建/隔离文件不可见/分支存在/无 remove 保留目录/同名冲突/带 remove 清理/非 git 目录明确报错）。剩余小项：meta.cwd 同步（会话列表仍归原项目）与 UI 入口
- [ ] **Scheduler**：CronCreate/List/Delete/Update 工具 + automations 表 + 独立 `bajin scheduler` 子命令
- [ ] **LSP 工具**（诊断查询，先支持 tsserver 单语言）
- [ ] `.agents/` 目录双前缀兼容（skills/commands/agents 与 .bajin 平行发现）
- [x] **自定义命令 frontmatter 生效**（2026-08-16 完成）：app-server send() 命中自定义命令时三效接线——model（≠当前则切会话模型并 rebuild）、allowed-tools（并入 SessionState.allowedTools + agent.allowTool 预授权）、skills（discoverSkills 命中即把 SKILL.md 正文（≤6k/个）以「[挂载技能 name]」段前注入 prompt，未安装静默跳过）；e2e 1 项三效断言（echo mock 回显含技能标记与展开体 + status.model 切换）；REPL 入口的同款接线留作小尾巴
- [x] **模型目录 + 自定义模型**（2026-08-14 人工完成）：内置 GLM 全系 24 个 + `models/add|remove|list` RPC + 持久化 `~/.bajin/config.json models[]`（保留其他键）+ 自定义端点独立 baseUrl/apiKey + 桌面端「模型」管理页（添加/删除/使用）+ 会话工具条下拉分组（自定义/内置/管理入口）+ 设置页（默认模型/模式 settings/set）+ 日志页（rollout 列表与尾部查看 logs/list|read）+ 左侧导航栏（会话/模型/设置/日志/帮助）——待 ZCode 截图对齐菜单条目与命名
- [x] **anthropic 兼容 provider**（2026-08-16 完成）：三处补齐——①CLI 直跑（REPL/headless）main.ts 接入与 app-server 同一条端点解析链（resolveModelEndpoint：模型自带 > 供应商 openai/anthropic 格式 > 全局 key；删除旧的全局 key 预检抢先退出）②anthropic provider URL 拼接改 SDK 惯例（baseUrl 无 /v 版本段自动补 /v1：api.anthropic.com→/v1/messages、open.bigmodel.cn/api/anthropic→/api/anthropic/v1/messages；带版本段不动）③settings.ts loadSettingsChain 用户级文件吃 BAJIN_HOME（修 loadConfig 在沙箱/迁移目录下读不到 config.model 的隐藏 bug）；cli-anthropic.e2e 1 项（本地 mock anthropic SSE 服务器：断言 /v1/messages+x-api-key+anthropic-version+流式回显）+ anthropic 单测断言更新。目录预设「智谱 GLM（Anthropic）」此前已存在，至此该预设全链路可用。剩余：/login OAuth（coding-plan 免 key 登录）属独立大项

### P2 —— 超越 ZCode（差异化）

- [ ] 多 provider：Anthropic / OpenAI / OpenRouter（env key + provider registry）
- [ ] 会话搜索与导出（跨会话 grep、导出 markdown）
- [ ] 精确 token 计数（tiktoken-cl100k 近似表）与成本估算显示
- [ ] 桌面端 e2e（playwright 驱动 AppImage）
- [ ] 中文/英文 UI i18n
- [ ] 性能：并行工具的输出流式回传（当前等全部完成）

## 运行日志（每次运行追加， newest 在底部）

| 时间 | 完成项 | 测试证据 | 下一步建议 |
|---|---|---|---|
| 2026-08-14 23:00 前置 | 账本初始化（人工） | 60/60 绿 | 从 P0-1 slash commands 开始 |
| 2026-08-14 23:30 人工 | 供应商体系（providers/add·remove·list，模型挂供应商，端点解析链：模型自带>供应商>全局）+ 侧边栏重构为 ZCode 结构（新建任务/搜索/自动化/技能/分组/项目/知识图谱 + 系统区）+ 六个新页面（跨会话搜索、自动化管理+每分钟调度器、技能管理、分组、项目聚合+目录选择、知识图谱占位）+ cron 引擎（5 字段 next-run）+ list-sessions 带 group/cwd | 76/76 绿（core 63 + cli 13）；打包态 providers/models/automations RPC 实测通过 | P0-1 slash commands；自动化调度器已具备，后续补 CronCreate 工具（agent 侧）与 scheduler 独立进程 |
| 2026-08-15 00:01 人工（截图对齐批次） | 按用户 ZCode 截图与参考 renderer i18n 串实现三块 UI：①使用统计页（usage/stats RPC：总 tokens/会话/消息/活跃天数/最常用模型占比/峰值时段/当前与最长连续天数/按天序列/模型分布，字符数÷3 估算；UsageView 含时间范围筛选 全部/7天/30天、指标卡、按天柱状图、模型占比条、GitHub 风格 12 周热力图；菜单新增「📊使用量」）②新建任务欢迎页（空会话居中：时段问候语 映射 ZCode chat.empty.greeting.*、模板卡 Git 站会摘要/CI 失败与不稳定测试/自定义、居中大输入框）③模型切换弹窗（会话栏 select 换成弹窗：按供应商分组列表+当前模型勾选+「管理模型与供应商」入口，对标 ZCode 模型下拉） | 77/77 绿（core 63 + cli 14，新增 usage/stats e2e）；打包态 bajin.cjs 实测 usage/stats(含 7d 筛选) 返回真实聚合；AppImage 重打 109MB | P0-1 slash commands（backlog 顶部不变）；后续可把使用统计接 rollout 精确 usage 替换估算 |
| 2026-08-15 00:10 夜间自动 | **P0-1 自定义 slash commands 完成**：core/commands.ts（发现顺序 用户>工作区逐级、冒号命名、flat frontmatter、$ARGUMENTS/$1..$9 展开与 User arguments 追加、动态 shell 拒绝）+ CLI REPL 接入 + app-server commands/list RPC 与 send 内展开 + 桌面斜杠补全合并与未知命令透传；规格按 ZCode zcode-guide diagnosing-commands 的 12 条 pitfall 逐条核对（大小写校验、缩进忽略、同名先到先得、.git 根停止扫描） | 87/87 绿（core 72 + cli 15；新增 commands.test.ts 9 项 + e2e 1 项）；bundle 737KB 打包态实测 commands/list + /greet 展开执行通过；AppImage 重打并验证打包态 RPC | P0-2 hooks 系统（7 事件 + matcher + 退出码协议）；P1 有命令 frontmatter 生效小项已挂回 |
| 2026-08-15 04:10 夜间自动 | **P0-2 Hooks 系统完成**（上轮中断于 app-server 接线，本轮收尾）：hooks.ts 全量实现 + Agent 7 事件接线（SessionStart 四来源/UserPromptSubmit 注入与阻止/PreToolUse 决策/PermissionRequest 再审批/PostToolUse 注入/PostToolUseFailure/Stop 续跑≤3）+ REPL/headless/app-server 三入口传参；修复两个 bug：spawn 参数重复传 shell 路径（126 退出码）、快速退出钩子的 stdin EPIPE 偶发未处理错误；规格按 zcode-guide diagnosing-hooks 12 条 pitfall 逐条核对 | 111/111 绿（core 95 + cli 16；新增 hooks.test.ts 15 项 + hooks-agent.test.ts 8 项集成 + e2e 1 项，core 4 连跑无 flake）；bundle 751KB 实测阻止/放行；AppImage 重打，打包态 UserPromptSubmit 注入实测通过 | P0-3 WebSearch/WebFetch 工具（免 key 源先行） |
| 2026-08-15 08:15 人工（用户报障修复） | **导航卡死修复**：侧边栏此前只在 chat 视图渲染，点进任意子页面（模型/设置/使用量等）后左侧菜单整体消失，无法切回——改为侧边栏常驻（任何页面可切换菜单），右侧内容区在 chat 与子页间切换；newTab/openHistory/newTabIn 现在都会 setView('chat') 回到会话；CSS flex 布局本就兼容（sidebar flex:none + view-page flex:1）无需改动 | 111/111 绿（本次仅渲染层改动）；打包内 app.asar renderer 与最新构建 md5 一致；AppImage 重打 | 用户验收中；后续子页面可加返回按钮与 Esc 返回 |
| 2026-08-15 08:25 人工（用户报障修复） | **模型页去重与精简**：改为供应商中心视图——模型只以 chips 形式展示在其供应商行内（供应商配置 models ∪ 挂靠该供应商的自定义模型，点 chip 即用）；删除「添加模型」表单与「内置模型（GLM 全系）」24 行列表（内置模型在会话页模型弹窗切换）；无供应商的旧自定义模型收进「独立端点模型（旧数据）」小节。顺带修 e2e 脆弱断言：providers 精确数量断言改「新增成员」断言 + 首尾清理（用户真实配置里有供应商后旧断言必挂） | 111/111 绿；打包内 renderer md5 与最新构建一致；AppImage 重打；用户真实 provider 配置（美团）完好 | 后端 models/add RPC 保留（无 UI 入口）；供应商行可再加「添加模型」内联输入 |
| 2026-08-15 09:00 人工（信息架构重设计） | **按 ZCode 信息架构系统性重构 UI**（用户批评「堆砌非产品设计」后深挖参考代码确认）：①侧边栏系统区收敛为单一「设置」入口，设置页改为左二级导航+右详情（基础：常规/模型设置；数据与统计：使用统计/日志；关于：帮助）——对齐 ZCode settings.nav 三分组结构；②「分组」「项目」不再是独立页面，改为任务列表视图切换（时间线/分组/项目，含 今天/昨天/本周/本月/更早 时间桶与相对时间显示），对齐 workspaceSidebar.organize；③模型设置改 ZCode 式：供应商卡片列表 + 「添加供应商」弹窗（供应商目录预设：智谱/DeepSeek/OpenRouter/Kimi/通义 | 自定义端点 两 tab），编辑同弹窗；④自动化/技能页改「列表卡片 + 弹窗创建」，表单不再常驻；⑤删除 GroupsView/ProjectsView/SearchView 外的冗余页面 | 111/111 绿；打包内 renderer md5 与最新构建一致；AppImage 重打 | 任务项上下文菜单（置顶/重命名/归档/移动分组）与侧边栏内嵌搜索待做；搜索页可改 Ctrl+K 弹窗 |
| 2026-08-15 08:50 人工（用户报障×3，参考代码驱动） | **模型体系三修**：①模型切换移入输入框（对标 ZCode composer：底栏 切换模式[默认模式/计划模式/接受编辑/完全访问，映射 mode.label.*] + 选择模型 + ↑发送；会话栏只留 tokens/压缩/分叉/停止）；ModelPicker 加搜索框与「管理模型」（对标 chat.toolbar.model.*）②修美团模型选不到：resolveModelEndpoint 对未注册模型按供应商 models 名单回退解析（带 apiFormat）；mergeModelOptions 把供应商名下模型并入 models/list③API 格式下拉（对标 settings.modelProvider.apiFormat）：ProviderEntry.apiFormat(openai|anthropic) 贯穿 添加弹窗(目录预设标注格式)/卡片展示/端点解析；新增 Anthropic Messages provider（x-api-key+anthropic-version、system 顶层、tool_use/tool_result 内容块、message_start/content_block_delta/message_delta SSE 解析、input_json_delta 拼参、stop_reason 映射、429/5xx 重试） | 120/120 绿（新增 anthropic.test.ts 7 项 + models 回退 2 项）；bundle 实测：添加供应商(两种格式)→models/list 并入名下模型→set-model 成功；AppImage 重打 md5 一致 | 推理强度档位（chat.toolbar.thoughtLevel）待接 GLM thinking 参数；composer 排队输入（followUpQueue）待做 |
| 2026-08-15 09:20 人工（对话体验重构，参考代码驱动） | **对话层重写（用户批评「没有思维链，只是聊天框」）**，词汇与结构全部取自参考 i18n：①思考块（chat.reasoning.*）：流式中「✻ 思考中...」带旋转动画，结束折叠为「思考过程（持续了 N 秒/几秒）」可点展开，Item 带时间戳②工具卡（chat.toolCall.*）：每工具动词化状态（读取中/已读取、执行中/已执行、编辑中/已编辑、写入中/已写入、搜索中/已搜索、更新待办/已更新待办、技能、子智能体），六态（等待中/执行中/已执行/执行失败/已拒绝/已停止），运行中显示实时耗时（busy 时每秒 tick），点击展开详情，Edit/Write 展开渲染 diff③助手消息加复制按钮（悬停显示，chat.message.copy）④右侧状态面板（chat.statusPanel.*）：目标（首条用户消息）/计划（审批中计划）/进程（todo，已完成项可折叠「已完成 N 项」）/会话（模型·模式·tokens），替换原底部 todo 面板 | 120/120 绿；打包态事件链实测 tool-call→tool-result→text-delta→done 完整；AppImage 重打 md5 一致 | thinking 依赖真实模型的 reasoning 输出（glm.ts 已解析）；后续：状态面板补 终端/智能体 区（后台任务就绪后）、大消息 toolSlice 分页 |
| 2026-08-15 09:35 人工（用户报障） | **修复「配了供应商 Key 仍降级 mock」**：bootstrap 原来只认全局 BIGMODEL_API_KEY/uc.bigmodel.apiKey，供应商 Key 被无视；现在 providers[] 里任一供应商配了 Key 即视为有可用凭据（不降级），且未显式配置默认模型时自动回落到第一个「有 Key 且有名下模型」的供应商模型；mock 提示文案更新并指路「设置→模型设置」。用户实际配置验证：美团(有Key, LongCat-2.0) → mock=False，默认模型自动 = LongCat-2.0 | 120/120 绿；打包内 main.cjs 含修复；AppImage 重打 | builtin 模型在无全局 Key 时发送会报清晰错误（引导用户切回供应商模型或配全局 Key），符合预期 |
| 2026-08-15 09:50 人工 | **建立 UI 对齐计划并完成批次1**：新建 UI-PLAN.md——从参考 i18n/命令注册表逐项盘点 ZCode 全部可交互元素（A 侧边栏 16 项 / B composer 8 项 / C 对话层 12 项 / D 状态面板 4 项 / E 设置页 8 项 / F 全局 5 项），标注 bajin 状态与 6 个迭代批次。批次1 落地：Ctrl+N 新建任务、Ctrl+K 搜索、Ctrl+W 关标签（全局 keydown）、侧边栏任务过滤框（标题/会话id）、工具卡「复制结果」、助手长消息(>1500字符)展开/收起、添加供应商弹窗「获取 API Key」外链（按 baseUrl 映射 7 家控制台） | 120/120 绿；AppImage 重打 | 批次2：任务项悬浮菜单（置顶/重命名/删除/移动分组，需 session/rename·delete·pin RPC）；批次4：Ctrl+K quickPick 命令面板 |
| 2026-08-15 10:00 人工（UI-PLAN 批次2） | **任务管理四件套**：app-server 新增 session/rename（meta.title，list 时优先于首条用户消息）、session/pin（meta.pinned，时间线模式「已置顶」桶置顶+📌 标记）、session/delete（删目录，若会话开着先关）、session/set-group 复用；渲染层 TaskListItem 悬浮菜单（⋯ 按钮）：重命名（内联输入，Electron 不支持 prompt）/置顶/移动到分组（留空取消）/删除（confirm）；新增 e2e 覆盖 rename→list 优先级、pin 双向、delete 后消失与不存在报错 | 121/121 绿（cli 17）；bundle 重打；AppImage 重打 | 批次3：归档 + 显示更多/收起；批次4：Ctrl+K quickPick + 消息重试 |
| 2026-08-15 10:30 人工（用户指漏：新建任务需基于文件夹） | **工作区选择器**（对标 chat.empty.workspaceMenu/selectProject/home/workOutsideProject）：composer 左侧与欢迎页各一个「📁 选择项目」chip，点开面板含 搜索工作区/最近项目（projects/list 按最近排序，显示路径与任务数）/选择文件夹（原生 pickDir）/主目录（bootstrap 新增 home）/不在项目中工作；选定文件夹后基于该 cwd 新建任务（session/new cwd → meta.json cwd → projects/list 聚合验证）；Tab 增加 cwd 跟踪（newTabIn/openHistory 带出），欢迎页文案对齐「开始在 {workspace} 项目新建任务」 | 121/121 绿；bundle 实测：session/new cwd=/tmp/ws-e2e → meta.cwd 正确、发消息后 projects/list 聚合到该项目；AppImage 重打 | 任务未发首条消息前不出现在项目列表（与 ZCode 行为一致）；后续可加最近工作区跨会话记忆 |
| 2026-08-15 11:10 人工（用户指页面不一致） | **统一 Composer**：欢迎页与会话页此前是两套输入框（欢迎页无模式/模型、工作区 chip 单独一行、Enter 直发；会话页要 Ctrl+Enter）——抽成单一 Composer 组件两处复用：textarea + 底栏（选择项目 | 模式 | 模型 | ↑），Enter 发送/Shift+Enter 换行（中文输入法 isComposing 保护），欢迎页为居中卡片态（640px 圆角卡片），移除 Ctrl/Enter 提示行 | 121/121 绿；打包内 renderer md5 一致；AppImage 重打 | 用户截图如有其他不一致点待下一轮对照修 |
| 2026-08-15 11:40 人工（用户指漏：选择项目缺远程） | **SSH 远程工作区（对标 workspaceSidebar.sshConnection* 的最小切片）**：选择项目面板新增「SSH 远程工作区」区——已配置列表（🖥 名称 user@host:port:/路径 + 删除）、「➕ 添加 SSH 连接」表单（名称/Host/Port/用户名/远程路径）；存储在 ~/.bajin/config.json remotes[]（主进程 remotes:list/add/remove IPC）；选中远程 → 主进程 kill 本地 agent，改用 ssh [port] user@host node <路径>/bajin.cjs app-server --stdio 拉起远程 agent（stdio 协议与本地完全一致，agent 跑在远程主机、文件操作落在远程 fs，架构对标 ZCode 远程运行时）→ 界面重载初始化；连接失败弹清晰前置条件说明（远程需 node + bajin.cjs 放配置路径） | 121/121 绿；AppImage 重打 | 远程前提：scp 本地 packages/cli/dist/bundle/bajin.cjs 到远程配置目录；后续可做自动部署 bajin.cjs 到远程（对标 ZCode remoteSync）与远程工作区连通性检测 |
| 2026-08-15 12:00 人工（用户报障：空会话两个输入框） | **修空会话双输入框**：统一 Composer 后欢迎页居中输入框与底部常驻输入框在空会话同时显示——改为互斥：空会话（无消息/不忙/无审批/无提问）只显示欢迎页的居中输入框，发出首条消息后切换为底部输入框（对标 ZCode：新建任务只有居中输入区，任务开始后只有底部输入区） | 121/121 绿；AppImage 重打 | — |
| 2026-08-15 12:35 人工（用户要求） | 会话内不再显示「bajin」发言人标识（对标 ZCode：消息流无产品名前缀，回复直接以正文呈现，仅保留悬停的 展开/收起/复制 操作） | 121/121 绿；AppImage 重打 | — |

| 2026-08-15 10:35 人工（用户要求：对标 ZCode 设计质感 + 头部空间利用率） | ①系统性视觉重设计：暖柔暗色配色（非纯黑高对比）/ 极细边框 / 统一大圆角体系 / 主按钮蓝色渐变填充 / 2px 微妙左边框任务项替代 4px 粗彩边 / 新建任务改为 ZCode 式蓝色药丸按钮 / 侧栏紧凑化（搜索框圆角化、视图切换紧凑）/ 移除 sessionbar 冗余 model·mode·tokens（已在 Composer+侧栏底部）/ 侧栏底部去重仅留 tokens+设置 / 任务项 hover 菜单改为浮层卡片（hover 才显 ⋯）/ 思考块工作时实时耗时 / 工具卡 expanded 状态类 | 121/121 绿；AppImage 重打 104MB | — |

| 2026-08-15 10:40 人工（用户要求：布局对标 ZCode，颜色留给设置里浅/深色调切换） | ①侧栏头部扁平化：移除 logo/mock/折叠 独占行，新建按钮 + 折叠按钮合成一行（新建撑满，折叠居右）②顶栏合并：tabbar + sessionbar 两条→单条 topbar（标签在左，压缩/分叉/停止在右，同一条 36px 高）③任务列表紧凑化：行距 7px→5px、分组标题留白缩减、字号微调④欢迎页下移：greet margin-top 12vh→7vh、字号微降 | 121/121 绿；AppImage 重打 104MB | — |

| 2026-08-15 10:57 人工（布局对标 ZCode，颜色不动） | ①空状态移除 4 张模板卡片（改为标题+描述+干净输入框，对标 ZCode 空状态）②任务项单行化（标题+时间同行 flex、div→span、📌 由 ::after 伪元素控制不占文档流）③视图切换修复（min-width:0 + overflow:hidden + 不折行，确保 时间线/分组/项目 三个按钮完整显示）④侧栏底部新增 build 标识（build 2→build 3）用于确认运行版本 | 121/121 绿；AppImage 重打 104MB | — |

| 2026-08-15 11:09 人工（顶栏标签对标 ZCode） | ①去掉顶栏白条（background 改为 --bg，去 border-bottom）②标签去掉分隔线（border-right）③标签内 busy 点去掉（对标 ZCode 无点）④标题居中（tab-title flex:1 + text-align:center）⑤关闭按钮 × 放右侧、常驻显示 opacity:.5 → hover 1 | 121/121 绿；AppImage 重打 104MB | — |

| 2026-08-15 11:28 人工（去掉顶栏白条） | 顶栏 background 改为 transparent（完全透明，与主区融为一体），标签/hover 无背景色，仅文字颜色变化 + 激活标签底部 1px accent 线 | 121/121 绿；AppImage 重打 104MB | — |

| 2026-08-15 11:35 人工（去掉顶部白条——根因修复） | 根因：白条是 Electron 原生系统标题栏，CSS 无法移除。对标 ZCode（titleBarStyle:"hidden"+titleBarOverlay:!0）：①BrowserWindow 加 titleBarStyle:'hidden' + titleBarOverlay:true，backgroundColor 更新 #18191d ②CSS topbar 加 -webkit-app-region:drag（承担窗口拖拽），tab/按钮 no-drag，右侧 110px 预留系统按钮 overlay ③build-tag 7 | 121/121 绿；AppImage 重打 104MB | — |

| 2026-08-15 11:45 人工（顶栏按钮对标 ZCode：压缩/分叉 → 终端/切换面板） | ①主进程新增集成终端 IPC：bajin:term:start/input/stop（spawn $SHELL --login，cwd=会话工作目录，stdout/stderr 流式经 term-data 事件回传，term-exit 通知）②preload 暴露 termStart/termInput/termStop③顶栏「压缩/分叉」替换为「⌗ 终端」「▤ 面板」（激活态 accent 高亮；/compact 命令保留压缩能力）④新增 TerminalPanel 组件（底部 240px：输出区 + $ 输入行，↑↓ 命令历史，自动滚底，关闭时 termStop）⑤右侧状态面板受「面板」按钮开关（showPanel，默认开） | 121/121 绿；AppImage 重打 104MB；build 8 | 非真实 TTY（vim/htop 等全屏程序不可用），后续接 node-pty |
| 2026-08-15 12:00 人工（侧栏去掉折叠，对标 ZCode 常驻展开） | 用户反馈「◀ 左三角不要，折叠后不能展开」——根因：折叠按钮在 .sidebar-head 内，.collapsed 把整个 head 连带按钮一起隐藏，故无法展开。ZCode 侧栏本身无折叠。修复：①移除 showHistory state 与 ◀▶ 折叠按钮 ②移除 .sidebar.collapsed 全部 CSS 规则（含 hidden 列表）③侧栏头部改为单行全宽蓝色「＋ 新建任务」（padding 简化，无 transition）build 9 | 121/121 绿；AppImage 重打 104MB；build 9 | 侧栏常驻展开，后续可考虑 icon rail 窄轨（非折叠） |
| 2026-08-15 12:12 人工（修复 build 9 误删 .icon-btn 样式） | 去掉折叠时误把 .icon-btn 与 .mock-badge CSS 一并删除，但 .icon-btn 被所有弹窗/面板的关闭 × 按钮使用（终端、创建定时任务、新建技能、添加供应商等）→ 界面残缺没法用。恢复 .icon-btn 样式（background:none/border:none/color:var(--dim)/hover 高亮），build 10 | 121/121 绿；AppImage 重打 104MB；build 10 | — |
| 2026-08-15 12:29 人工（简洁统一：新建任务并入菜单 + 修历史会话无输入框） | ①「新建任务」不再是特殊大蓝按钮——并入 side-menu 成为普通菜单项（side-item-accent 轻微 accent 文字色），去掉 sidebar-head/.new-chat 特例样式（用户要求：简洁统一不要特例，利于维护）②修「打开历史会话后没有输入框」：chat-row/status-panel 两个 class 一直无 CSS 定义，消息流无高度约束把 composer 挤出屏幕——补 chat-row{flex:1;display:flex;min-height:0}、status-panel{280px 右栏}、sp-section/sp-title/sp-goal/sp-plan 样式 build 11 | 121/121 绿；AppImage 重打 104MB；build 11 | 后续自查：改 CSS 时 grep 所有 className 是否有样式定义 |
| 2026-08-15 14:20 人工（新建任务页对标 ZCode + Enter 修复 + 停止按钮下移） | 深挖参考 renderer 确认 ZCode 布局后三修：①修 Enter 不发送：初始标签页 sessionId=null 时 send() 直接 return——新增 ensureSession() 首发自动创建会话②欢迎页对标 chat.empty：问候语 + rounded-2xl 单卡片 composer（工作区 chip + textarea + 模式/模型/发送 一体）+ 4 个 pill 推荐卡（站会摘要/修测试/写测试/脚手架，点击填入 prompt）③停止按钮从顶栏移到 composer 右下（busy 时替换 ↑，红色，Esc 也可停止）④对话页 composer 同样改为单卡片（去两条横线），focus 时 accent 边框 build 15 | 121/121 绿；AppImage 重打 104MB；build 15 | — |
| 2026-08-15 14:30 人工（截图对照：只读工具紧凑单行 + 顶栏图标化） | 用户贴 ZCode/bajin 对照截图，两大差异修复：①只读工具（Read/Grep/Glob/LS/LSGrep）从大卡片改为紧凑单行条目（icon + 工具名 + 参数摘要，24px 高，成功绿/失败红/运行转圈，点击展开输出）——修改类（Edit/Write/Bash/Agent）保留卡片样式②顶栏「⌗ 终端」「▤ 面板」文字按钮改为纯图标（icon-only，title 提示保留）。产品文档 https://zcode.z.ai/en/docs/welcome 已记录为权威参考 build 16 | 121/121 绿；AppImage 重打 104MB；build 16 | 剩余对照差异：计划/todo 进度内嵌消息流、输入框下方模式提示行 |
| 2026-08-15 16:59 人工（窗口按钮去白底 + 顶栏左侧工作区下拉） | ①titleBarOverlay 从 true 改为 {color:'#1d1f24', symbolColor:'#9aa0aa'}——最小化/最大化/关闭按钮去白色底，融入深色主题②顶栏最左新增工作区下拉选（复用 WorkspaceChip：当前项目名 + ▾，下拉切换/选择文件夹/主目录/SSH，与 composer 内一致；no-drag 区域已处理）③思考块默认折叠（active 也不强制展开，随时点击）build 17+18 | 121/121 绿；AppImage 重打 104MB；build 18 | — |
| 2026-08-15 19:50 人工（任务列表去时间线 + 设置体系迁移第一批） | ①任务列表视图只留 分组/项目（时间线移除，置顶桶两种模式都置顶，默认分组）②设置体系对标 ZCode settings.general/agentCapabilities 第一批（全部真实生效，存 ~/.bajin/config.json settings 块，主进程 config:get/set + notify + hooks IPC）：基础设置 8 项——界面语言(存偏好)、任务完成通知(Electron Notification)、通知声音(开关)、消息流显示思考(隐藏 ThinkingBlock)、消息流显示待办(隐藏 todo 面板)、自动归档(超期任务按天数过滤列表、置顶豁免)、集成终端 Shell(auto/bash/zsh/sh，主进程按配置 spawn)、终端字体(即时应用)③新增 Agent 设置分区：hooks 总开关+钩子列表(读写 config.json hooks 块，事件中文名映射)+技能/自动化/知识图谱入口+MCP/子代理/记忆/插件「规划中」标注④Switch 开关组件 + hooks-list CSS build 19 | 121/121 绿；AppImage 重打 104MB；build 19 | 下一批：MCP 服务器 UI、hooks 编辑表单、语言 i18n 全量、通知声音播放 |
| 2026-08-15 20:35 人工（设置迁移第二批：MCP/hooks表单/代理/数据目录/i18n/子代理） | ①Hooks 编辑表单（事件下拉 7 项中文名/matcher/命令/超时，添加+删除，直写 config.json hooks 块）②MCP 服务器管理 CRUD（stdio command+args / sse url，存 config.json mcpServers；agent 运行时连接明确标注下一批）③网络代理卡（httpProxy/noProxy/caCert——主进程 spawn agent 注入 HTTPS_PROXY/NO_PROXY/NODE_USE_ENV_PROXY=1，Node24 原生 fetch 走代理，重启生效）④数据目录迁移卡（选目录→复制 sessions/rollout→记 dataDir；cli 的 sessions/rollout/skills 全部支持 BAJIN_HOME 环境变量覆盖，重启后 agent 用新目录）⑤子代理信息卡（Explore/general-purpose 内置说明，自定义定义标注规划中）⑥i18n 基础设施（t()+EN 词典 50+ 条，语言切换即时重渲染）：覆盖侧栏菜单/视图切换/设置导航/欢迎页/composer 提示/常用按钮，其余逐步补 build 22 | 121/121 绿；AppImage 重打 104MB；build 22 | 剩余：MCP 运行时连接（spawn server + mcp__server__tool）、自定义子代理定义、i18n 全量词条、通知声音 |
| 2026-08-15 20:40 人工（MCP 运行时接入：stdio 全链路） | core/mcp.ts 净室实现 stdio 传输：McpStdioClient（spawn 子进程 + newline JSON-RPC：initialize 握手/notifications/initialized/tools-list/tools-call，超时与进程退出兜底）+ connectMcpServers（多 server 并联，单个失败仅告警不拖垮整体，工具包装为 mcp__<server>__<tool> 进审批体系 readOnly=false/riskLevel=medium）；agent.initContext 主会话自动连接（子代理跳过防递归），sessionClose 调 disposeMcp 释放子进程；新增 mcp.test.ts 3 项（假 stdio server e2e：工具注入/参数透传/isError 映射、坏配置隔离、空配置） | 124/124 绿（core 107 + cli 17，新增 mcp 3 项）；AppImage 重打 104MB；build 23 | sse 传输、MCP OAuth、资源/提示词能力下一批 |
| 2026-08-15 20:42 人工（自定义子代理 + 通知声音） | ①core/subagents.ts：发现 ~/.bajin/agents/*.md（用户级）与 <cwd>/.bajin/agents/*.md（项目级覆盖同名），flat frontmatter name/description/tools（可选，缺省全集）+ 正文=该子代理系统指引②Agent 工具 subagent_type 从固定枚举放开为运行时校验字符串：自定义类型按定义限制工具集（mcp__ 工具始终放行）+ 注入 description+body 作为 promptSuffix，未知类型返回可用清单；发现后重建工具描述把自定义类型列给模型③app-server subagents/list RPC + 桌面 Agent 设置动态列表（名称/描述/工具/级别 + 使用说明）④通知声音真实生效：done 时 WebAudio 合成 660→880Hz 双音（无需音频文件）；新增 subagents.test.ts 2 项（frontmatter 解析/项目级覆盖/空目录） | 126/126 绿（core 109 + cli 17）；AppImage 重打 104MB；build 24 | — |
| 2026-08-15 20:50 人工（Memory 记忆系统 + 模式提示行） | ①core/memory.ts：两级存储（用户 <BAJIN_HOME>/memory/MEMORY.md + 项目 <cwd>/.bajin/memory/MEMORY.md，条目 `- [时间] 文本`）+ readMemories/saveMemory/clearMemories/memoryPromptBlock②Memory 工具（save/recall/list，scope 选择）：模型在会话中自主记录稳定偏好与项目事实，保存后 refreshMemory 即时注入 systemPrompt③agent 接线：initContext 读取 + systemPrompt 追加「用户长期记忆/项目记忆」块 + 主会话注册工具④app-server memory/list·clear RPC + 桌面 Agent 设置记忆卡（条目列表 + 按级清空）⑤composer 模式提示行：plan=计划模式蓝色提示（不改动文件）、yolo=完全访问黄色警示（截图对照 ZCode 遗留项）；新增 memory.test.ts 3 项 | 129/129 绿（core 112 + cli 17）；AppImage 重打 104MB；build 25 | 剩余：MCP sse/OAuth、i18n 全量、插件市场 |
| 2026-08-15 21:05 人工（MCP sse 传输） | core/mcp.ts 抽 McpClientLike 接口，新增 McpSseClient（legacy HTTP+SSE：GET 订阅事件流收 endpoint 事件定回传地址 → 请求 POST JSON → 响应经 message 事件回流；AbortController 断流兜底、headers 鉴权透传）；connectMcpServers 统一分发 stdio/sse 并包装 mcp__<server>__<tool>；桌面 MCP 卡更新为「stdio + sse」；新增 mcp-sse.test.ts 2 项（本地假 SSE server 全链路：endpoint→握手→工具→调用；不可达地址隔离不拖垮） | 131/131 绿（core 114 + cli 17）；AppImage 重打 104MB；build 26 | MCP OAuth 与资源/提示词能力仍留待后批 |
| 2026-08-16 00:59 夜间自动 | **P0-3 WebSearch/WebFetch 完成** + 本轮前置两件事：①用户要求建立本地 git 防护——bajin/ 初始化独立仓库（85 文件，node_modules/release 由 .gitignore 排除），checkpoint `de31b60` 全量快照；硬性约束更新为「全绿后本地 commit、禁 push」②用户要求定时窗口 23:00-04:00 → 21:00-07:00：ZCode 内部存储无手改入口（仅日志可见），已给用户界面修改指引（cron `*/10 21-23,0-6 * * *`），本工具集无 CronUpdate 代改能力③web.ts 实现（见 backlog 勾选项详述）：DuckDuckGo 免 key 搜索 + HTML→文本确定性转换 + 超时/大小上限 + provider 可关；fetchImpl 全注入 7 项测试不触网；顺手勾掉过时的 MCP client 行（build 23/26 已完成） | 138/138 绿（core 121 + cli 17，新增 web.test.ts 7 项）；bundle 789KB；本地 commit 本轮追加 | 下一轮：backlog 顶部 SQLite 会话库（node:sqlite + --rewind）或 settings 作用域链 |
| 2026-08-16 01:10 夜间自动 | **SQLite 会话库条目之 rewind 切片完成**：core/session.ts 新增 rewindTranscript（一轮=一条 user 消息及其后 assistant/tool 块；从文件尾部定位第 N 条 user 行裁掉其后全部原始行，不重序列化；损坏行不误判轮起点；n 超界清空、n<=0/负数 no-op、文件不存在安全返回）+ CLI `--rewind <n>`（正整数校验；配合 --resume/-c 对最近或指定会话回退；仅裁 transcript，meta.json 不动；--help 补充说明）；core index 导出 rewindTranscript；rewind.test.ts 4 项（回退1轮内容核对/越界清空/no-op 原样/损坏行与缺失文件）。SQLite 迁移本体留 backlog 顶部 | 142/142 绿（core 125 + cli 17，新增 4 项）；bundle 791KB；CLI 变更已 bundle；本地 commit 追加 | 下轮：SQLite 会话库本体（node:sqlite 建库 + loadTranscript/listSessions 兼容层 + JSONL 迁移），或若嫌大先做 settings 作用域链 |
| 2026-08-16 01:16 夜间自动 | **SQLite 会话库之 SessionStore 切片完成**：core/session-store.ts 全新模块——node:sqlite 经 createRequire 惰性解析（三个动机：vitest/vite 不能静态解析 node: 前缀、CJS bundle 顶层 createRequire(import.meta.url) 会炸、Electron 内置 Node 缺该模块时不能拖垮 core 整体导入，调用点才报清晰错误）；五表 schema（WAL、idx_message_session、外键 ON DELETE CASCADE）；storeAppendMessage 事务写入并拆 part（toolCall=assistant.toolCalls、toolResult=tool 消息）+ tool_usage 统计；storeLoadTranscript 复刻压缩标记重置语义；migrateJsonlToStore 幂等迁移（session 已存在整目录跳过、meta 缺失兜底、损坏行与 loadTranscript 同策略）。core index 导出全部 API。修两处 TS（ToolCall.arguments 非 args、SQLOutputValue 行集转 unknown） | 146/146 绿（core 129 + cli 17，新增 session-store.test.ts 4 项）；bundle 792KB 且 require 冒烟通过；本地 commit 追加 | 下轮接线切片：app-server 初始化时 BAJIN_HOME/sessions.db 开库 + 首启迁移 + loadTranscript/listSessions 走 store（JSONL 双写一个过渡期）；再下 `bajin migrate` 子命令与 todo 表 live 写入 |
| 2026-08-16 01:28 夜间自动 | **SQLite 会话库切片3：bajin migrate 子命令**：CLI 新增 `bajin migrate [--db <file>]`（app-server 同级子命令；默认 <BAJIN_HOME>/sessions.db，遵循 BAJIN_HOME 覆盖；openSessionStore+migrateJsonlToStore 复用切片2 API，finally close，失败 exitCode=1）；--help 补「子命令」段。**关键修复**：session-store 惰性 createRequire 在 CJS bundle 里 import.meta.url 被 esbuild shim 后失效（被 try/catch 误报为「不支持 node:sqlite」）——两级回退 createRequire(import.meta.url)→createRequire(process.execPath)（内置模块解析与路径无关），错误信息分离（require 失败 vs 模块缺失）。migrate.e2e.test.ts 2 项：seed JSONL→migrate→断言输出计数+库可回放+幂等重跑 skip；空 BAJIN_HOME 安全成功 | 148/148 绿（core 129 + cli 19，新增 e2e 2 项）；bundle 重打并 BAJIN_HOME 沙箱冒烟通过；本地 commit 追加 | 下轮：接线最后一步——app-server 侧 listSessions/loadTranscript 改走 store + Agent persist 双写（JSONL 过渡）、todo 表 live 写入；完成后勾掉整个 SQLite 条目 |
| 2026-08-16 01:40 夜间自动 | **SQLite 会话库切片4：写路径双写**：①agent.ts AgentOptions.storeSink 钩子——persist()（JSONL 后）与 compact 压缩标记双入口并行入 store，sink 异常只吞不阻断（JSONL 为读路径主源，过渡期零风险）②app-server：进程级惰性 sessionStore()（首次调用开 <BAJIN_HOME>/sessions.db，失败告警一次降级纯 JSONL——Electron 内置 Node 缺 node:sqlite 也不影响会话）；createSession 里 upsert session 行 + storeSink 闭包经 sidHolder 承载构造后的 sessionId、每条消息顺带 UPDATE modified_at③session 表补 group/pinned/modified_at：新库建表带上、旧库自动 ALTER（失败=已存在忽略）；pinned 可空（NULL=未提供），upsert 用 COALESCE/CASE 保证未显式提供不覆盖旧值；迁移器读 meta 原始 group/pinned，modifiedAt 取 transcript 最后消息 ts。修一处自伤：NOT NULL pinned 与 NULL-语义冲突改可空 | 151/151 绿（core 132 + cli 19，新增 session-dualwrite.test.ts 3 项：双写回放逐条一致/新列 upsert 语义/旧库自动补列）；bundle 重打；app-server e2e 无回归；本地 commit 追加 | 下轮读路径收官：listSessionsEnriched/session/open 切 store + rename/pin/set-group RPC 同步写 + todo live 写入，然后勾掉整个条目 |
| 2026-08-16 08:45 夜间自动（续上轮中断点） | **SQLite 会话库切片5：元数据/清理闭环**（上轮改到一半被截断，本轮先核工作区→补完）：①core 新增 storeUpdateSessionMeta（部分键更新、group null=清除、pinned 布尔映射）/storeDeleteSession（PRAGMA foreign_keys=ON 级联清 message/part/todo/tool_usage）/storeReplaceTodos+storeLoadTodos（todo 快照 DELETE+批量 INSERT 事务）②app-server 四处接线：set-group 与 rename/pin（sessionMutateMeta）写 meta.json 后同步 store；sessionDelete 删目录后 storeDeleteSession；bindCallbacks 的 todo-updated 处 storeReplaceTodos（todo 表 live 写入落地）；listSessionsEnriched 加 store 覆盖层——title/group/pinned 以 store 行优先（双写期 store 最新），store 不可用回退纯 JSONL 行为完全不变 | 154/154 绿（core 135 + cli 19，新增 session-meta.test.ts 3 项：部分键更新语义/todo 整表替换/级联删除三表计数归零）；bundle 重打；本地 commit 追加 | SQLite 条目只剩「全切换收尾」（session/open 主读写切 store + JSONL 停写开关），可择机做；下轮建议转 backlog 下一项 settings 作用域链 |
| 2026-08-16 08:52 人工（续：settings 作用域链完成） | core/settings.ts 全量实现（发现/深合并/Env 覆盖三件套，见 backlog 勾选项详述）；cli loadConfig 从「用户级+cwd 两文件」升级为完整链：项目发现到 .git 根、双文件名、BAJIN_* Env 层，--model/--mode 旗标天然为 Cli 层；删除 config.ts 冗余 mergeDeep；USAGE 补作用域链说明。行为增强不破坏旧配置（用户级/cwd bajin.json 语义原样保留，只是项目层更完整） | 157/157 绿（core 138 + cli 19，新增 settings.test.ts 3 项）；bundle 重打；本地 commit 追加 | 下轮：backlog 顶部「后台任务」（Bash run_in_background + TaskOutput/TaskStop）或 SQLite 全切换收尾 |
| 2026-08-16 09:00 人工（用户批评「工作无法被检查」——修正轮） | 根因两条：①夜间多轮全做内核项（SQLite/配置链/rewind）无 UI 暴露②只 bundle CLI 未重打 AppImage，用户运行的仍是 build 26 旧包。修正：①session/rewind RPC（裁 JSONL + store 删该会话后按裁后内容重导，保持双写一致）+ config/chain RPC（loadSettingsChain 发现结果诊断）②UI 两个可见入口：任务悬浮菜单「⏪ 回退 1 轮并打开」（确认后回退并直接打开会话）、设置→常规「配置作用域链」卡（用户级/项目级逐文件+级别徽章+环境覆盖键+优先级提示）③全量 package.sh 重打 AppImage=build 27，并解包 asar 验证 build 27/config.chain/session.rewind/新卡文案全部在包内（注意 esbuild 对非 ASCII 做 \\uXXXX 转义，验证需按转义串匹配）④硬性约束新增「用户可检查性」规则 | 157/157 绿；AppImage 104MB 重打并解包验证；本地 commit 追加 | 夜间轮次今后按新规则执行；下轮建议 P1 UI 项（如 composer 斜杠命令面板）恢复可感知节奏 |
| 2026-08-16 09:32 人工（用户指令：Agent 菜单严格照抄 ZCode，不自行发挥） | 设置页 Agent 组拆为 8 个独立分区，与 ZCode agentCapabilities 顺序一一对应：记忆/插件/技能/子代理/自动化/MCP/命令/钩子。拆分后各页：记忆=MemoryCard、插件=市场页（诚实标注规划中）、技能=复用 SkillsView、子代理=内置两个+SubagentList、自动化=复用 AutomationsView（新增 onOpenSession 透传——点任务跳回对话页）、MCP=McpCard、钩子=HooksCard、命令=新页（commands/list RPC 列出 name/description/来源徽章 + 空态指引 ~/.bajin/commands）。删除旧混合式 AgentSection；SettingsSection 类型扩 8 值；i18n 补 8 词条；AutomationsView 跳转经 SettingsView 新 prop 接 App 的 openHistory | 157/157 绿；AppImage 重打 build 28 并解包验证 8 个 section id+中文标签全部在包内；本地 commit 追加 | 后续：插件页从「规划中」变真功能时对标 settings.plugins.* 词条；命令页可加「打开命令目录」按钮 |
| 2026-08-16 09:52 人工（用户指令：侧栏分组/项目区与任务菜单照抄 ZCode） | ①任务视图区照抄截图：[＃ 分组][🗂 项目] 带图标 tab + ▾ 选项菜单（排序 按更新/按创建 单选、收起/展开全部分组、显示归档任务开关，全部对应 workspaceSidebar.* 真实键），分组头可点击折叠（▾/▸）②任务 ⋯ 菜单照抄 ZCode taskList 13 项与顺序：置顶/重命名/归档/标记为未读｜分屏/文件管理器｜复制 路径/任务路径/日志路径/会话ID｜前往配置/调用轨迹/反馈；每项接真实行为——新增 session/archive·unread RPC（meta 持久化，list 过滤归档、打开自动清未读●标记）、bajin:reveal-path（shell.showItemInFolder）与 bajin:open-external IPC、listSessions 富字段（createdAt/archived/unread/sessionDir/rolloutPath）；前往配置→设置·模型设置、调用轨迹→设置·日志、反馈→外链；删除自创的「移动到分组/删除/回退1轮」菜单项（回退功能保留在 RPC，删除功能移出菜单）③i18n 21 词条 | 157/157 绿；AppImage build 29 重打并解包验证 17 项标识全部在包内；本地 commit 追加 | 删除任务功能暂无入口（ZCode 在归档视图），需要时恢复；「在分屏打开」当前=本标签打开（真分屏待 P1） |
| 2026-08-16 10:20 人工（用户报障：agent 无法自己创建定时任务） | 根因：自动化只有"人配置"入口（设置页 RPC），agent 无工具。修复：①automations 存储从 cli 迁 core（BAJIN_HOME 语义统一：显式 home 参数=homedir（拼 .bajin），无参=BAJIN_HOME 根 ?? ~/.bajin；修掉两层 .bajin 重复 bug），cli/automations.ts 保留 re-export②core/tools/cron.ts 四工具对标 ZCode：CronCreate（cron 5 字段或 delayMinutes 一次性，title 保留自然语言时间描述、prompt 自包含）/CronUpdate（改 cron 重算 nextRun、enabled 暂停）/CronDelete/CronList，注册 builtinTools——模型在会话里说"每晚写一章小说"即可自己建任务，设置→自动化立即可见③调度器 tick 支持 oneShot（触发后自动停用）④e2e 彻底沙箱化：startServer 注入 BAJIN_HOME 临时目录，测试进程读 persisted 同沙箱、删除真实 ~/.bajin 清理块——用户真实数据（含其手建 4 个自动化）绝不触碰；skills/commands/models/mcp/subagents 五模块用户级目录全部接入 BAJIN_HOME | 160/160 绿（core 141 + cli 19，新增 cron-tools.test.ts 3 项）；bundle 800KB；AppImage build 30 重打并解包验证 4 工具在包内 CLI bundle；本地 commit 追加 | 验证路径：对话里让 agent「每晚上8点写一章小说」→ 它调 CronCreate → 设置→Agent 能力→自动化 出现条目并按点触发 |
| 2026-08-16 14:15 人工（用户：技能列表为空，参考 ZCode 给默认技能） | core/skills.ts 新增 BUILTIN_SKILLS 五件套（净室自写正文，能力域对标 ZCode 官方插件）：skill-creator（技能创建指南）/docx（python-docx 报告方案）/pptx（python-pptx 汇报设计基线）/pdf（LaTeX·HTML·pypdf 选型与坑）/self-check（命令·钩子·MCP·技能四类不生效的排查流程）+ seedBuiltinSkills（仅缺失写入、用户改过永不覆盖、删了可自愈）；app-server initialize 幂等种入——桌面首启后技能列表即有内容，Skill 工具描述同步可见；e2e 沙箱内种入不影响断言 | 163/163 绿（core 144 + cli 19，新增 builtin-skills.test.ts 3 项：种入发现/幂等不覆盖/删后自愈）；AppImage build 31 重打并解包验证 seedBuiltinSkills+五技能名在包内；本地 commit 追加 | 用户已装的技能不受影响；后续可再加 browser-use 域默认技能 |
| 2026-08-16 14:35 人工（用户：技能查看方式难受 + 数量差距，对标 ZCode 交互与完整度） | ①内置技能 5→10（对标 zcode-guide 诊断家族）：+diagnosing-commands/hooks/mcp/skills + configuration-guide（净室自写排查流程正文）②SkillsView 照抄 ZCode 交互：搜索框过滤名/描述、本地技能分组（计数徽章）、卡片=🛠名+项目级/用户级徽章+描述+启用开关（Switch）、点卡片展开详情面板（状态/作用域/路径 + 查看正文/在文件管理器中打开 + 内置标注"删除后重启可恢复"）、新建技能弹窗保留③启用/禁用真实生效：config.json skillsDisabled 持久化 + skills/toggle RPC + skills/list 带 enabled/file + Agent disabledSkills 过滤（系统提示清单隐藏 + Skill 工具执行拒绝并指路设置）——app-server initialize 载入清单、createSession 注入 | 163/163 绿；AppImage build 32 重打并解包验证（10 技能名/skills.toggle/新交互文案在包内）；本地 commit 追加 | 数量说明：ZCode 另有 browser-use/股票/小说等域技能属其插件市场内容，非默认；bajin 默认集=通用十件套，市场功能在插件页规划中 |
| 2026-08-16 14:50 人工（用户：迁移 ZCode 基础组四项设置） | 基础组从 2→4 项，顺序照抄 ZCode basics：常规/外观/模型设置/浏览器。①外观（settings.appearance）：界面主题 浅色调/深色调/跟随系统——styles.css 加 [data-theme='light'] 全套变量（底色/面板/文字/边框/主色/语义色/代码底/阴影 22 变量），渲染层 theme→根节点 data-theme 即时生效、system 模式监听 prefers-color-scheme；主进程 config:set-settings 联动 setTitleBarOverlay（窗口按钮随主题变色）——兑现用户早期诉求「颜色在设置里通过浅/深色调解决」②浏览器（settings.browser）：清理缓存（session.clearCache）/清除所有站点数据（clearStorageData+clearCache，confirm 保护）——Electron session 真实清理，非摆设 | 163/163 绿；AppImage build 33 重打并解包验证（light 主题/浅色调文案/清理 IPC 在包内）；本地 commit 追加 | 浅色主题首版变量如对比度不适可微调；ZCode browser 的「导入站点数据/浏览器控制开关」依赖内嵌浏览器面板，归入后续浏览器功能批 |
| 2026-08-16 15:05 人工（用户三连反馈：正文没人看到/开关分不清/没有自定义技能） | ①查看正文改为居中弹框（modal-overlay + skill-content-modal，60vh 滚动 + 关闭按钮），不再沉底②Switch 状态对比修复：开=实心主色轨道+白钮、关=灰轨道+深灰钮（原 accent-soft 轨道在浅色主题下与关闭态几乎同色，两态肉眼不可辨——这次两主题下都拉开）③技能分组拆为 自定义技能/内置技能 两组（各带计数）；自定义组空态给引导（右上新建按钮或手写 ~/.bajin/skills/<名>/SKILL.md），消除"没有自定义技能"的困惑（新建流程本身 e2e 已覆盖且正常） | 163/163 绿；AppImage build 34 重打并解包验证；本地 commit 追加 | Switch 若仍觉不显眼可再加尺寸；正文弹框后续可加"编辑"直存 |
| 2026-08-16 15:15 人工（内功修炼：后台任务，P0 内核清零） | core/background.ts + tools/tasks.ts：BackgroundTaskManager（32 任务上限/环形缓冲尾部 64KB/SIGTERM→2s SIGKILL）；Bash run_in_background 立即返 taskId+前5行输出；TaskOutput（block 等结束含退出码、非阻塞快照）+ TaskStop（重复停/未知 id 明确反馈），注册 builtinTools——模型可「后台起 dev server→轮询→用完停掉」。至此 P0 内核条目（slash 命令/hooks/Web 工具/MCP client/SQLite 会话库/settings 链/后台任务）全部完成 | 167/167 绿（core 148 + cli 19，新增 background.test.ts 4 项）；bundle 重打；AppImage build 35；本地 commit 追加 | 下一步内功排序：SQLite 全切换收尾（读切 store+JSONL 停写开关）或 P1 自定义命令 frontmatter 生效（allowed-tools 预授权/model 切换，解析已完成只差接线） |
| 2026-08-16 19:06 人工（内功：自定义命令 frontmatter 生效） | send() 命中 /cmd 时 frontmatter 三效接线：model=glm-x 与当前不同→s.model 切换+rebuild（status 可回读）；allowed-tools=Bash,Read→并入会话 allowedTools+agent.allowTool 运行期预授权（免审批语义）；skills=fm-skill→SKILL.md 正文 ≤6k/个 以「[挂载技能 name]」前缀注入 prompt（未装跳过）；command-frontmatter.e2e.test.ts 沙箱（BAJIN_HOME）建命令+技能→send /deploy→echo mock 回显断言技能标记与展开体+status.model 断言。至此 P1 该项完成；REPL 入口同款接线为剩余小尾巴 | 168/168 绿（core 148 + cli 20，新增 e2e 1 项）；bundle 重打；AppImage build 36；本地 commit 追加 | 下轮内功候选：SQLite 全切换收尾（读切 store/停写开关）或 REPL frontmatter 接线 + anthropic provider（bigmodel anthropic 端点） |
| 2026-08-16 20:05 人工（内功：anthropic 兼容端点全链路） | ①main.ts CLI 直跑接端点解析链（此前仅 app-server 支持 anthropic 格式供应商；顺带删掉旧全局 key 预检）②providers/anthropic.ts URL 智能拼 /v1（SDK 惯例，目录预设无版本段也能打对路径）③修 settings 链用户级文件不吃 BAJIN_HOME 的隐藏 bug（loadConfig 沙箱下 model 回退默认导致供应商解析空）——e2e 排查时逐层定位：key 缺失→model 未读到→路径根源；cli-anthropic.e2e 用本地 mock SSE 服务器全链路断言（路径/头/流式文本） | 169/169 绿（core 148 + cli 21，新增 e2e 1 项+断言更新）；bundle 重打；AppImage build 37；本地 commit 追加 | /login OAuth（coding-plan 登录态）为剩余独立大项；下轮候选：SQLite 全切换收尾 或 REPL 命令 frontmatter 接线 |
| 2026-08-16 20:20 人工（用户：设置交互照 ZCode——侧栏同位置替换，非子菜单） | 旧交互问题：设置导航是右侧内容区里的子侧栏（settings-shell 双栏），会跟随详情滚动、换菜单要划回来。照 ZCode 改：设置态下 aside.sidebar 整体替换为设置导航（同一位置切换）——顶部「← 返回任务」回 chat，下方分组导航（settings-sidebar-scroll 自身滚动区，不随详情动）；SettingsView 剥掉自带 aside 只留 settings-detail（独立滚动）。修一处由此引入的 JSX 失衡（删壳留了双闭合 div → esbuild「未结束的正则」报错定位）。旧 settings-shell/settings-nav CSS 保留兼容未删 | 169/169 绿；AppImage build 38 重打并解包验证（settings-back/build 38 在包内）；本地 commit 追加 | 验证：点设置→左栏整体变设置菜单、不随右侧滚动；返回任务一键回 |
| 2026-08-16 20:40 人工（用户：新建会话未立即出现在项目分组） | 根因两层：①core listSessions 要求 transcript.jsonl 存在才认会话——新建未发消息的空会话被跳过。改为 meta.json 存在即算（transcript 可选；空会话补读 meta.json 取 cwd/title、modifiedAt 用 meta mtime 兜底、title 链 meta.title>首条 user>「(新会话)」）——项目分组（按 meta.cwd）与任务列表立即可见②前端 newTab 创建后未刷新列表（newTabIn 此前已带）——补 void refreshHistory()。早期「空会话不进列表与 ZCode 一致」的旧结论作废（用户实测 ZCode 新建即显示）。session-list.test.ts 1 项：空会话立即出现+空目录跳过+正常会话不受影响 | 170/170 绿（core 149 + cli 21，新增 1 项）；AppImage build 39 重打并验证；本地 commit 追加 | 项目分组现含未发消息会话，projectsList 计数随之即时更新 |
| 2026-08-17 夜间自动（续完用户报障：模式切换失效/不灵活） | 根因：set-mode 走 withSessionAsync 的 busy 检查——任务运行中切换被拒，UI 静默吞错（本地先 patch 了 tab 造成"看似切了实际没切"）。修复：①app-server setMode 绕过 busy 门槛直取会话（agent.setMode 本就设计为运行中即时生效，对标 ZCode 随时可切）②UI 换 ZCode 式模式菜单 ModeMenu：按钮+向上弹出菜单（四模式名称+一行说明+当前项 ✓，任意时刻可点），替换原 busy 时禁用的原生 select③e2e 新增 1 项：send 执行 sleep 工具期间 set-mode 成功且 status 回读生效（修测试自身两处：误伤审批测试的锚点、信封断言） | 171/171 绿（core 149 + cli 22，新增 busy-set-mode e2e 1 项）；AppImage build 40 重打并解包验证（mode-menu/mode-trigger/转义中文均在）；本地 commit 追加 | 验证路径：任务运行中点模式菜单切"完全访问"→立即生效不停任务 |
| 2026-08-17 00:35 夜间自动 | **EnterWorktree/ExitWorktree 完成**（P1 git 隔离实验）：Agent.setCwd（opts.cwd+refreshSystem，工具上下文即时跟随）+initialCwd；Enter 建 .bajin-worktrees/<name> 与分支 bajin/<name>（基线可指定）切入，Exit 回原目录（remove=true 清理工作树+分支）；主仓库根用 --git-common-dir 解析（修 worktree 内 --show-toplevel 返回自身的坑）；动态注册（子代理不重复挂）。worktree.test.ts 2 项覆盖隔离性/同名冲突/清理/非 git 报错 | 173/173 绿（core 151 + cli 22，新增 2 项）；bundle 853KB；AppImage build 41 重打并解包验证 EnterWorktree 在包内；本地 commit 追加 | 剩余小项：meta.cwd 随 worktree 同步（列表归组）+ 桌面 UI 入口（任务菜单「在 worktree 中打开」）；下轮候选：文件树面板 或 图片 Read |
| 2026-08-17 00:55 夜间自动 | **图片 Read 完成**（P1）：core/tools/image.ts 纯头解析（不解码像素）四格式 + readTool 扩展名分支返回占位描述（含多模态预留文案）；修两处：①全局长度守卫 16B 误杀 10B GIF 头→各格式自带守卫②测试构造器 JPEG 段布局算错（len 含自身 2B，段总长=2+len）——以解析器为准重排 | 176/176 绿（core 154 + cli 22，新增 image-read.test.ts 3 项）；bundle 重打；AppImage build 42 重打并解包验证 parseImageSize 在 cjs、build 42 在 asar；本地 commit 追加 | 下轮候选：AskUserQuestion multiSelect 全链路 或 桌面文件树面板 |
| 2026-08-17 01:15 夜间自动 | **multiSelect 全链路打通**：排查发现 shared 类型/工具 schema/事件透传三层早备好，唯一断点在桌面 ask-card 只渲染单选按钮。补 AskMultiCard（复选状态+确认按钮+已选计数，答案「、」拼接；样式 picked 高亮）+ 类型补字段；e2e 增 multiSelect 变体（事件断言→多选回传→tool-result 含拼接） | 177/177 绿（core 154 + cli 23，新增 e2e 1 项）；bundle 重打；AppImage build 43 重打并解包验证 ask-multi/ask-check/build 43 在包内；本地 commit 追加 | 下轮候选：桌面文件树面板（P1 体验）或 SQLite 全切换收尾 |
| 2026-08-17 02:40 夜间自动×2（补上轮中断报告 + 本轮修复收尾） | 上轮（SQLite 读路径容灾）在 e2e 调试中被截断未写报告，工作区留下失败测试——本轮先复现：独立探针证明 store 写入正常（2 行消息），真根因是 requireInit 的 sessions.size 判定让「关掉唯一会话后 session/open」被误拒；修复=requireBoot/initializedOnce + e2e 透出 error；探针与 e2e 双验证（删 transcript.jsonl 后 open 仍从 store 恢复完整历史）。**流程检讨**：夜间轮输出预算见底时应先记账「中断点+现场」再结束，不留无报告的失败树 | 178/178 绿（core 154 + cli 24，新增容灾 e2e 1 项）；bundle 重打；AppImage build 44；本地 commit 追加 | SQLite 条目仅剩可选「JSONL 停写开关」；下轮候选：文件树面板 / 精确 token 计数 |
| 2026-08-17 21:05 人工（用户：迁移插件与浏览器功能） | **插件系统 + 浏览器面板完成**：①core/plugins.ts——插件发现（~/.bajin/plugins/<名>/plugin.json 清单 + skills/commands 子目录扫描）、启停（togglePlugin 写回 enabled）、安装（installPlugin 从本地目录复制 + 自动生成 manifest）、pluginSkillDirs/pluginCommandDirs 提供 enabled 插件的额外扫描根；skills.ts/commands.ts 发现链尾部追加插件目录（source: 'plugin'，优先级最低，同名冲突用户/项目级覆盖插件）②app-server RPC：plugins/list·toggle·install③桌面 Agent→插件 页从占位变真实（列插件名/版本/描述/技能/命令 + Switch 启停 + 空态指引）④浏览器面板：BrowserPanel（webview + URL 栏 + 关闭按钮，底部 320px 抽屉）+ 顶栏🌐按钮切换 + IPC bajin:browser:navigate（BrowserNavigate 工具让模型控制面板打开 URL）+ BrowserContent 工具（获取当前页面文本，无面板时降级 WebFetch）；BrowserNavigate/BrowserContent 注册 builtinTools | 182/182 绿（core 158 + cli 24，新增 plugins.test.ts 4 项）；AppImage build 45 解包验证 browser-panel/BrowserNavigate/plugins RPC 均在包内；本地 commit 追加 | 插件市场（远程 git 安装）后续；浏览器面板的 BrowserContent 目前依赖注入桥接，桌面端可扩展 setBrowserBridge 连通 webview |
| 2026-08-17 21:12 人工（用户：会话加删除按钮） | 任务 ⋯ 菜单末尾加「删除任务」（danger 红色 + confirm 确认 + session/delete RPC——此前照抄 ZCode 13 项时移除了删除，用户反馈需要）；菜单变 14 项（ZCode taskList.delete 键存在，归位） | 182/182 绿；AppImage build 46 重打并验证；本地 commit 追加 | — |
| 2026-08-17 21:50 人工（用户：自动化卡片 + 项目短名） | ①自动化页改独立卡片（automation-card：⏰/⚡图标+标题+cron pill+状态行+prompt 摘要（2行截断）+操作栏（会话/暂停/删除），禁用半透明）②项目分组改文件夹名（bucketTasks 键从全路径 cwd 改 split('/').pop() 短名，同名不同路径自动加 (全路径) 区分；分组头加 📁 图标） | 182/182 绿；AppImage build 47 重打并验证；本地 commit 追加 | — |
| 2026-08-17 21:58 人工（用户：项目目录照 ZCode 卡片布局，别创新） | 项目视图整重做：每个项目一张带边框卡片（对标 ZCode workspace-group rounded-lg border bg-card）——卡片头（▾折叠 + 📁 + 项目名 + 任务数 pill）、路径行（10.5px mono 半透明、折叠时隐藏）、任务缩进卡内（左缩 20px）；分组视图保持文字分组头不变；bucketTasks 键回退为全路径（上一轮改短名是错误方向，路径在卡内单独行展示更符合 ZCode） | 182/182 绿；AppImage build 48 重打并验证 project-card 在包内；本地 commit 追加 | 用户提到的"还有功能"待明确具体缺什么 |
| 2026-08-17 23:10 夜间自动 | **REPL frontmatter 三效接线完成**（P1 小尾巴清零）：repl.ts 自定义命令展开处补 model 提示 / allowed-tools allowTool 预授权 / skills SKILL.md 正文注入（与 app-server send() 同逻辑：≤6k/个前缀 [挂载技能 name]）；repl-fm-integration.test.ts 1 项（命令发现→frontmatter 字段可达→技能正文可读→注入+展开链完整——不依赖 REPL 进程的集成测试更稳）；REPL e2e 管道测试因 stdin 时序脆弱已弃 | 183/183 绿（core 159 + cli 24，新增 1 项）；bundle 重打；AppImage build 49；本地 commit 追加 | P1 剩余：文件树面板、LSP 工具、.agents 双前缀；下轮候选：会话导出 markdown（P2） |
| 2026-08-17 23:15 夜间自动 | **会话导出 Markdown 完成**（P2 该项清零）：core/session-export.ts exportSessionMarkdown——ChatMessage[]→可读 .md（标题+模型/ID/cwd 元信息 / 👤用户 / 🤖助手 / 工具调用折叠 details 块 / 系统消息省略 / tokens 尾注）；CLI 子命令 `bajin export <id> [--out file]`（前缀匹配 sessionId，默认 <sessionId>.md，遵循 BAJIN_HOME）；--help 补充说明。session-export.test.ts 3 项（完整结构/空列表/无工具） | 186/186 绿（core 162 + cli 24，新增 3 项）；bundle 重打；AppImage build 50 解包验证 exportSessionMarkdown 在 cjs、build 50 在 asar；本地 commit 追加 | 下轮候选：文件树面板（P1 体验）/ .agents 双前缀 / 精确 token 计数 |
