| 2026-08-15 12:20 人工（用户报障：打开历史任务不见对话记录） | **历史会话完整还原**：session/open 此前只把历史装进 agent 上下文，渲染层拿不到消息（只见「已恢复 N 条」提示+空输入区）——现在返回非 system 消息列表；渲染层 historyToItems 还原完整对话流：user/assistant 直接映射，assistant.toolCalls 生成工具卡占位、role:tool 消息按名回填真实输出（配对队列），对话打开即与 ZCode 一样显示全部往来（含可展开的工具详情）；移除「已恢复 N 条」系统提示 | 121/121 绿；bundle 实测 session/open 返回 [user,assistant,tool,assistant] 完整序列；AppImage 重打 | 长会话可考虑分页加载（对标 chat.message.toolSlice） |
| 2026-08-15 12:45 人工（用户要求） | **选择项目移到输入框上方**：工作区 chip 从 composer 底栏移至输入框顶行（下方展示不全、弹出被裁切），选择面板改为向下展开（chip 在上、向下空间充足，z-index 120）；底栏保留 模式｜模型｜↑ 发送 | 121/121 绿；AppImage 重打 | — |
| 2026-08-15 13:00 人工（用户要求） | **侧边栏结构对齐 ZCode**：去掉头部系统菜单区（SIDE_SYSTEM），设置按钮移至侧边栏右下角（底部状态行右侧 ⚙，激活高亮，点击切换设置/会话，再次点击返回）；底部状态行模式显示改中文标签 | 121/121 绿；AppImage 重打 | — |
| 2026-08-15 13:20 人工（用户要求，参考实现核实） | ①隐藏系统菜单栏：参考 main 进程核实 ZCode 用 autoHideMenuBar:true（非 setApplicationMenu(null)），同样配置——File/Edit/View 默认不显示（Alt 唤出），窗口顶部直接是内容（侧栏折叠 ◀▶ + 新建任务）②思考块工作时显示实时耗时：active 态头部「思考中... Xs」（复用 busy 每秒 tick）；结束仍默认折叠为「思考过程（持续了 N 秒）」点击展开（chat.reasoning.duration* 语义） | 121/121 绿；AppImage 重打 | — |
